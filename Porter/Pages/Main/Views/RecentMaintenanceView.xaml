<UserControl
    x:Class="Porter.Pages.Main.Views.RecentMaintenanceView"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:Porter.Util.ViewModels"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d"
    d:DesignHeight="300"
    d:DesignWidth="400">

    <Border Style="{StaticResource BorderStyle}" Width="350">
        <StackPanel HorizontalAlignment="Center">
            <TextBlock Text="Most Recent Maintenance" HorizontalAlignment="Center" />
            <TextBlock x:Name="MaintenanceName" HorizontalAlignment="Center" FontSize="20" />
            <Grid>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*" />
                    <ColumnDefinition Width="*" />
                </Grid.ColumnDefinitions>
                <TextBlock x:Name="Cost" FontSize="16" HorizontalAlignment="Center"  Margin="5" />
                <TextBlock x:Name="Mileage" Grid.Column="1" FontSize="16" HorizontalAlignment="Center"  Margin="5" />
            </Grid>
            <TextBlock x:Name="Reminder" HorizontalAlignment="Center" />
        </StackPanel>
    </Border>
</UserControl>
